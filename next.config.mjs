/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['eu-west-2.graphassets.com'], // Add the allowed domain here
      },
    
}


;

export default nextConfig;
